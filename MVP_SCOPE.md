# MVP 범위 구체화 — 칼로리 트래커

관련 문서: [PRD.md](PRD.md) · [TECH_SPEC.md](TECH_SPEC.md)

이 문서는 PRD의 범위(3장)를 실제 구현 착수 기준으로 더 구체화한 체크리스트다. "Must / Should / Won't"로 우선순위를 나눠서, 시간이 부족하면 Must만으로도 배포 가능한 앱이 되도록 구성했다.

> v2.0 변경: 회원가입/로그인(Supabase Auth)과 기록의 Supabase DB 저장이 Must Have로 승격됨 (기존 Won't Have에서 이동).
> v3.0 변경: "날짜별 히스토리(달력) 조회"와 "목표 칼로리 설정"이 Won't Have에서 Must Have로 승격됨 (PRD v4.0 반영). 실제 구현 진행 상황에 맞춰 완료된 항목을 [x]로 표시함.

---

## 1. Must Have — 이번 MVP에 반드시 포함

### 1-1. 인증 (Supabase Auth)
- [x] 이메일/비밀번호 회원가입 화면
- [x] 이메일/비밀번호 로그인 화면
- [x] 로그아웃 버튼 (헤더에 로그인 이메일 + 로그아웃)
- [x] 비로그인 상태에서는 트래커 화면 접근 불가 → 로그인 화면으로 전환
- [x] 세션 유지 (새로고침해도 로그인 상태 유지)
- [x] Supabase 에러(중복 가입, 잘못된 비밀번호 등)를 사용자 친화적 메시지로 표시
- [x] 비밀번호 정책: 8자 이상 + 복잡도 요구(클라이언트) + 유출 비밀번호 차단(Supabase HaveIBeenPwned 연동, 대시보드 설정)
- [ ] CAPTCHA(Cloudflare Turnstile) 프론트엔드 연동 (Supabase 대시보드 설정은 별도 진행 중, 코드 반영 대기)

### 1-2. 화면/인터랙션 (트래커)
- [x] 음식 이름 검색창 (입력 시 실시간 필터링)
- [x] 검색 결과 리스트 (최대 8~10개까지만 노출)
- [x] 음식 선택 시 100g 기준 칼로리/탄/단/지 미리보기 표시
- [x] 섭취량(g) 입력창 — 음식 선택 시 `commonServingG` 값으로 자동 채움, 수정 가능
- [x] 실시간 계산 미리보기
- [x] "기록에 추가" 버튼 — 음식 미선택 또는 섭취량 0 이하일 때 비활성화
- [x] 오늘의 기록 리스트 (Supabase에서 로그인 사용자 + 오늘 날짜 기준 조회)
- [x] 각 기록 항목 삭제 버튼 (Supabase에서 실제 삭제)
- [x] 상단 요약: 오늘 총 칼로리 / 탄수화물(g) / 단백질(g) / 지방(g)
- [x] 기록이 없을 때 빈 상태 안내 문구

### 1-3. 데이터
- [x] `foods.json` — 114개 음식 데이터 (한식/배달음식/프랜차이즈 포함), 정적 파일로 유지
- [x] Supabase `logs` 테이블 생성 (user_id, food_id, food_name, amount_g, calories, carbs, protein, fat, log_date, created_at) — TECH_SPEC 3.3 참고
- [x] `logs` 테이블 RLS 활성화 + select/insert/delete 정책 (본인 데이터만 접근) — TECH_SPEC 3.4 참고

### 1-4. 저장/동기화
- [x] 로그인 계정 기준으로 기록이 Supabase DB에 저장됨
- [x] 같은 계정으로 다른 기기/브라우저에서 로그인 시 동일한 오늘 기록 조회 (재접속/새로고침 기준, 실시간 아님)
- [x] 날짜가 바뀌면 새 날짜 기준 빈 기록으로 자연스럽게 전환 (`log_date` 컬럼 기준 조회)

### 1-5. 배포
- [x] GitHub 새 저장소 생성 — [rubilisa08/my-product_MVP](https://github.com/rubilisa08/my-product_MVP)
- [x] Supabase 프로젝트 생성
- [x] `supabase/setup.sql` 실행하여 테이블/RLS 적용
- [x] 프론트엔드는 정적 파일(HTML/CSS/JS/JSON)만으로 구성, 빌드 단계 없음
- [x] GitHub Pages로 배포하여 공개 URL로 접속 가능 — https://rubilisa08.github.io/my-product_MVP/
- [ ] 배포 전 서로 다른 두 계정으로 교차 확인 (RLS 검증)

### 1-6. 목표 칼로리 & 캘린더 (PRD v4.0 추가)
- [x] 목표 칼로리(하루 총 칼로리) 설정/수정 UI — Supabase Auth user_metadata에 저장 (TECH_SPEC 3.5)
- [x] 목표 미설정 시 안내 문구 표시, 이 경우 캘린더에 노란색 표시 없음
- [x] 캘린더 화면: 요일별 그리드의 실제 월력, 이전/다음 달 이동
- [x] 날짜별 총 칼로리 숫자 표시 (기록 있는 날짜만)
- [x] 목표 초과 날짜 노란색 표시 / 이내는 기본색 / 기록 없음은 무강조
- [x] 오늘 날짜 시각적 구분 표시
- [x] 날짜 클릭 시 해당 날짜 기록 상세 조회 (읽기 전용 — 삭제/수정 버튼 없음)
- [x] 기록 없는 날짜 클릭 시 "이 날은 기록이 없어요" 안내

---

## 2. Should Have — 여유 있으면 포함 (없어도 MVP 성립)
- [x] 반응형 레이아웃 (모바일 화면에서도 깨지지 않게)
- [ ] 최근 추가한 음식 "즐겨찾기/최근 사용" 상단 노출
- [ ] 섭취량 단위를 g 외에 "1인분" 버튼으로 빠르게 선택
- [x] 로그인/회원가입 폼 기본적인 유효성 검사 (이메일 형식, 비밀번호 확인 일치, 비밀번호 복잡도)

---

## 3. Won't Have (이번엔 제외, 향후 로드맵)
- 비밀번호 재설정(이메일 발송), 소셜 로그인(Google 등)
- 사용자가 새 음식을 직접 등록하는 기능
- 외부 공공 영양성분 API 연동
- 주간/월간 통계 그래프 (평균 추이 등 — 달력 색상 표시로 대체)
- 탄수화물/단백질/지방 개별 목표 설정 및 달성률
- 과거 날짜 기록의 수정/추가 (조회만 가능)
- 목표 변경 이력 관리 (소급 적용됨, PRD 8.1)
- 실시간 동기화 (Supabase Realtime)
- 다국어 지원, 다크모드
- 프로필 수정, 회원 탈퇴 UI

---

## 4. 완료 기준 (Definition of Done)
아래를 모두 만족하면 이번 MVP는 "완성"으로 본다.

1. 이메일/비밀번호로 회원가입 → 로그인 → 트래커 화면 진입까지 오류 없이 동작한다.
2. 음식 검색 → 선택 → 섭취량 입력 → 추가 → 삭제까지 전체 플로우가 오류 없이 동작하며, 실제로 Supabase DB에 반영된다.
3. 새로고침/재로그인해도 오늘 기록이 유지된다.
4. 서로 다른 두 계정으로 각각 로그인했을 때, 상대방의 기록이 절대 보이지 않는다 (RLS 검증 완료).
5. 오늘 기록의 총합(칼로리/탄/단/지)이 개별 항목 합과 항상 일치한다.
6. 모바일 브라우저(가로 360~400px)에서 레이아웃이 깨지지 않고 사용 가능하다.
7. GitHub Pages 배포 URL로 접속했을 때 로컬과 동일하게 동작한다 (Supabase 연결 포함).
8. 목표 칼로리를 설정하면, 달력에서 목표를 초과한 날짜가 정확히 노란색으로 표시된다.
9. 과거 날짜의 기록 상세 화면에는 삭제/수정 버튼이 없다 (조회 전용).

---

## 5. 다음 단계 (착수 순서 제안)
1. ~~Supabase 프로젝트 생성~~ ✅ 완료
2. ~~`supabase/setup.sql` 작성 및 실행 (logs 테이블 + RLS)~~ ✅ 완료
3. ~~로컬 프로젝트 뼈대 생성~~ ✅ 완료
4. ~~`foods.json` 데이터 작성 (현재 114개)~~ ✅ 완료
5. ~~회원가입/로그인/로그아웃 화면 및 로직 구현~~ ✅ 완료
6. ~~검색 + 계산 + 추가/삭제 로직을 Supabase 연동으로 구현~~ ✅ 완료
7. ~~로컬 서버 확인 + GitHub Pages 배포~~ ✅ 완료 — https://rubilisa08.github.io/my-product_MVP/
8. 목표 칼로리 설정 UI 구현 (user_metadata 저장/조회, TECH_SPEC 3.5/5.9)
9. 캘린더 화면 구현 (월별 로그 조회, 날짜별 색상 판정, TECH_SPEC 5.10~5.11)
10. 날짜 클릭 시 상세 조회(읽기 전용) 구현 (TECH_SPEC 5.12/6.8)
11. 로컬에서 목표 설정 → 여러 날짜 기록 → 달력 색상까지 수동 테스트
12. GitHub Pages 재배포 및 최종 확인
13. (별도 트랙) CAPTCHA 프론트엔드 연동 — Supabase Attack Protection 설정과 짝을 맞춰 진행
