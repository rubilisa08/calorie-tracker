# PRD (Product Requirements Document) — 칼로리 트래커 MVP

관련 문서: [MVP_SCOPE.md](MVP_SCOPE.md) (기능 체크리스트) · [TECH_SPEC.md](TECH_SPEC.md) (기술 설계)

## 0. 문서 정보
- 작성일: 2026-09-08
- 버전: v3.0 (Supabase 회원가입/로그인 반영)
- 작성자: Lisa
- 상태: 초안 — 리뷰 후 확정되면 구현 착수
- 저장소: [rubilisa08/my-product_MVP](https://github.com/rubilisa08/my-product_MVP)

### v3.0 변경 요약
- 로그인 구현을 직접 만들지 않고 **Supabase Auth(이메일/비밀번호)** 에 위임하기로 결정.
- 로그인 도입에 따라 **음식 기록(로그) 데이터도 브라우저 localStorage 대신 Supabase DB에 저장**하여 여러 기기에서 동기화되도록 범위를 확장.
- 이에 따라 이전 버전(v2.0)에서 "2단계 이후"로 미뤘던 회원가입/로그인/클라우드 동기화 항목이 이번 MVP 범위로 앞당겨짐.

---

## 1. 제품 개요

### 1.1 제품명 (가칭)
칼로리 트래커 (Calorie Tracker)

### 1.2 한 줄 정의
로그인 후, 오늘 먹은 음식을 검색해서 섭취량만 입력하면 칼로리와 탄수화물·단백질·지방(탄단지)을 자동 계산해 하루 총량을 보여주고, 어떤 기기에서 접속해도 동일한 기록을 볼 수 있는 웹 앱.

### 1.3 비전 (장기 방향)
"기록의 번거로움 때문에 식단 관리를 포기하지 않도록" 만드는 가장 가벼운 칼로리 트래커. 계정 기반으로 기록을 안전하게 보관하고, 이후 풍부한 음식 DB와 통계 기능으로 확장한다.

### 1.4 배경 및 문제 정의
- 식단을 관리하려는 사람은 매 끼니마다 (1) 음식의 100g당 영양성분을 검색하고 (2) 실제 먹은 양에 맞춰 손으로 비율 계산을 하고 (3) 하루치를 다시 합산해야 하는 3단계 수작업을 거친다.
- 기존 다이어트/식단 앱은 기능이 많고 UI가 복잡해서, "오늘 뭘 얼마나 먹었는지"만 빠르게 확인하고 싶은 사람에게는 진입장벽이 있다.
- 개인 기록이므로 기기를 바꿔도(휴대폰 ↔ PC) 같은 기록을 볼 수 있어야 하며, 이를 위해서는 계정(로그인) 기반 저장이 필요하다.
- 로그인 기능은 보안이 중요한 영역이므로 직접 구현하기보다 **검증된 인증 서비스(Supabase Auth)** 를 사용해 비밀번호 저장/암호화, 세션 관리 등의 위험을 줄인다.

### 1.5 타겟 사용자 / 페르소나

**페르소나 A — "가볍게 확인하고 싶은 나"**
- 특징: 매일 엄격하게 식단을 재는 건 아니지만, 오늘 하루 대략 얼마나 먹었는지 감을 잡고 싶음
- 니즈: 간단한 가입 후, 음식 검색 → 몇 번의 클릭/입력만으로 기록 완료
- 사용 빈도: 하루 2~4회 (끼니/간식마다), PC와 휴대폰을 오가며 사용

**페르소나 B — "운동/다이어트를 막 시작한 사람"**
- 특징: 단백질 섭취량 등 탄단지 비율에 관심이 생겼지만, 정밀한 앱은 부담스러움
- 니즈: 오늘 먹은 것들의 탄/단/지 합계를 한눈에 보고 싶고, 어제/오늘 기록이 기기와 상관없이 남아있길 원함
- 사용 빈도: 다이어트/운동 기간 동안 매일

이번 MVP는 이 두 페르소나가 공통으로 원하는 "빠른 입력 + 자동 계산 + 하루 합계 확인 + 기기 간 동기화"에 집중한다.

---

## 2. 목표 및 성공 지표

### 2.1 제품 목표
1. **빠른 기록**: 음식 1건을 기록하는 데 걸리는 시간을 최소화한다.
2. **정확한 계산**: 입력한 섭취량(g)에 정확히 비례한 칼로리·탄단지를 계산한다.
3. **한눈에 파악**: 하루 총 섭취량(칼로리/탄/단/지)을 별도 조작 없이 즉시 확인할 수 있다.
4. **안전한 개인 계정**: 검증된 인증 서비스로 회원가입/로그인을 제공하고, 본인 기록만 본인이 볼 수 있도록 보장한다.
5. **기기 간 동기화**: 로그인하면 어느 기기에서 접속하든 동일한 기록을 확인할 수 있다.

### 2.2 성공 지표 (MVP 기준, 정량)
| 지표 | 목표 |
|---|---|
| 음식 1건 기록 소요 시간 | 10초 이내 (검색 시작 ~ 리스트 반영까지, 로그인 이후 기준) |
| 계산 오차 | 0% — 섭취량 비례 계산은 항상 정확해야 함 (반올림 오차 제외) |
| 초기 음식 데이터 커버리지 | 자주 먹는 한식/기본 음식 기준 80~100개 등록 |
| 회원가입~로그인 성공 | 이메일/비밀번호로 가입 후 즉시(또는 이메일 인증 후) 로그인되어 트래커 화면 진입 가능 |
| 데이터 격리 | 다른 사용자의 기록이 절대 보이지 않음 (RLS로 검증) |
| 기기 간 동기화 | PC에서 추가한 기록이 새로고침 없이도(또는 재접속 시) 모바일에서 동일하게 조회됨 |
| 배포 상태 | GitHub Pages 배포 URL로 외부에서 접속 가능 |
| 브라우저 호환 | 최신 Chrome, Safari(모바일 포함)에서 정상 동작 |

### 2.3 이번 MVP가 아닌 것 (Non-goals)
- 의학적으로 정밀한 영양 관리 도구가 되는 것 (참고용 추정치 제공이 목적)
- 다양한 음식/브랜드 제품까지 모두 포괄하는 것 (2단계 이후)
- 비밀번호 재설정, 소셜 로그인(Google 등), 이메일 인증 커스터마이징 등 고급 인증 기능 (Supabase가 기본 제공하지만 이번 MVP에서 화면/플로우까지 구현하지는 않음)

---

## 3. 범위 (Scope)

### 3.1 MVP 포함 (In Scope)
- **Supabase Auth 기반 회원가입 / 로그인 / 로그아웃** (이메일 + 비밀번호)
- 로그인하지 않은 사용자는 트래커 화면에 접근할 수 없고, 로그인/회원가입 화면으로 안내됨
- 자체 제작 음식 데이터베이스 (정적 JSON, 100g 기준 정규화된 영양성분 — 로그인 불필요, 공용 데이터)
- 음식 검색 및 선택
- 섭취량(g) 입력에 따른 칼로리/탄수화물/단백질/지방 자동 계산
- **오늘 하루 기록을 Supabase DB(logs 테이블)에 사용자 계정 기준으로 저장/조회/삭제**
- 하루 총합(칼로리/탄/단/지) 실시간 표시
- **로그인 상태 유지 및 여러 기기에서 동일 계정으로 접속 시 동일 기록 조회**
- 날짜가 바뀌면 자동으로 새 하루 기록으로 전환
- 반응형(모바일 포함) 레이아웃
- GitHub Pages를 통한 정적 웹 배포 (프론트엔드만 배포, 백엔드는 Supabase 관리형 서비스 사용)

### 3.2 MVP 제외 (Out of Scope → 향후 로드맵, 9장 참고)
- 비밀번호 재설정(이메일 발송), 소셜 로그인(Google/Kakao 등)
- 사용자가 새 음식을 직접 등록/편집하는 기능
- 외부 영양성분 공공 API 연동 (음식 종류 확장)
- 지난 날짜 히스토리 조회(달력), 주간/월간 통계 및 그래프
- 목표 칼로리/영양소 설정 및 달성률 표시
- 다국어 지원, 다크모드
- 모바일 네이티브 앱화
- 프로필 수정, 회원 탈퇴 UI (계정 삭제는 Supabase 대시보드에서 수동 처리)

세부 기능 우선순위(Must/Should/Won't)는 [MVP_SCOPE.md](MVP_SCOPE.md)에 체크리스트로 정리되어 있다.

---

## 4. 기능 요구사항 (User Stories & 상세 인수 조건)

### US-1. 회원가입
**As a** 신규 사용자 **I want to** 이메일과 비밀번호로 가입 **So that** 내 기록을 저장할 계정을 만들 수 있다

- 이메일, 비밀번호(및 비밀번호 확인) 입력 필드 제공.
- Supabase Auth의 `signUp` 기능을 사용하며, 비밀번호는 우리 앱 코드가 직접 다루거나 저장하지 않는다 (Supabase에 위임).
- 가입 성공 시: (Supabase 프로젝트 설정에 따라) 이메일 인증이 필요하면 안내 문구 표시, 인증이 필요 없으면 즉시 로그인 상태로 전환.
- 이미 가입된 이메일로 재가입 시도 시 Supabase가 반환하는 에러 메시지를 사용자에게 알기 쉬운 문구로 보여준다.
- 비밀번호 최소 조건(Supabase 기본값, 예: 6자 이상)을 충족하지 못하면 에러 안내.

### US-2. 로그인
**As a** 기존 사용자 **I want to** 이메일과 비밀번호로 로그인 **So that** 내 기록에 다시 접근할 수 있다

- 이메일/비밀번호 입력 후 로그인 버튼 클릭 시 Supabase Auth `signInWithPassword` 호출.
- 로그인 성공 시 트래커 메인 화면으로 이동, 로그인 실패(비밀번호 오류 등) 시 에러 메시지 표시.
- 로그인 세션은 브라우저에 유지되어, 같은 브라우저로 재방문 시 자동 로그인 상태 유지 (Supabase 클라이언트 라이브러리 기본 동작).

### US-3. 로그아웃
**As a** 로그인한 사용자 **I want to** 로그아웃 **So that** 공용 기기에서 내 계정을 보호할 수 있다

- 화면 상단에 로그아웃 버튼(및 현재 로그인된 이메일 표시) 제공.
- 클릭 시 Supabase 세션 종료 후 로그인 화면으로 이동.

### US-4. 음식 검색
**As a** 사용자 **I want to** 음식 이름을 검색 **So that** 매번 영양정보를 직접 찾지 않아도 된다

- 검색창에 텍스트를 입력하는 즉시 결과가 필터링된다 (부분 문자열 매칭).
- 결과가 없을 때: "검색 결과가 없어요. 다른 이름으로 검색해보세요" 안내 표시.
- 결과가 많을 때: 최대 8~10개까지만 노출.

### US-5. 음식 선택 및 섭취량 입력
**As a** 사용자 **I want to** 음식을 고르고 실제 먹은 양을 입력 **So that** 그 양에 맞는 영양정보를 얻는다

- 검색 결과에서 음식을 클릭하면 100g당 기준 영양정보가 요약 표시된다.
- 섭취량 입력창은 선택 즉시 해당 음식의 `commonServingG` 값으로 자동 채워지며, 자유롭게 수정 가능.
- 섭취량 0 이하 또는 미입력 시 "추가" 버튼 비활성화.

### US-6. 계산 미리보기 및 기록 추가
**As a** 사용자 **I want to** 추가하기 전에 계산 결과를 먼저 확인 **So that** 잘못된 값으로 기록되는 것을 방지한다

- 선택한 음식 + 섭취량 기준으로 칼로리(정수), 탄/단/지(소수점 1자리)를 실시간 계산해 미리보기로 표시.
- "기록에 추가" 클릭 시 로그인된 사용자 계정으로 Supabase `logs` 테이블에 새 행이 저장된다.
- 저장 성공 시 오늘의 기록 리스트에 즉시 반영, 검색/입력 상태는 초기화되어 다음 음식을 바로 검색할 수 있다.
- 저장 실패(네트워크 오류 등) 시 에러 안내 및 재시도 가능해야 한다.

### US-7. 오늘의 기록 확인
**As a** 사용자 **I want to** 오늘 기록한 항목을 목록으로 확인 **So that** 무엇을 얼마나 먹었는지 되짚어볼 수 있다

- 로그인 시, 오늘 날짜에 해당하는 본인 계정의 기록만 Supabase에서 조회하여 리스트로 표시.
- 각 항목에는 음식명, 섭취량(g), 칼로리, 탄/단/지가 표시된다.
- 같은 음식을 여러 번 추가해도 개별 항목으로 각각 표시된다.
- 기록이 없을 때: "오늘 기록된 음식이 없어요" 빈 상태 문구 표시.

### US-8. 하루 총합 확인
**As a** 사용자 **I want to** 오늘 섭취한 총 칼로리/탄/단/지를 확인 **So that** 하루 전체를 파악한다

- 화면 상단에 총 칼로리(kcal), 총 탄수화물(g), 총 단백질(g), 총 지방(g)을 표시.
- 리스트에 항목이 추가/삭제될 때마다 총합이 즉시 재계산되어 갱신된다.

### US-9. 기록 삭제
**As a** 사용자 **I want to** 잘못 추가한 기록을 삭제 **So that** 정확한 하루 기록을 유지한다

- 각 리스트 항목에 삭제(×) 버튼 제공.
- 클릭 시 Supabase `logs` 테이블에서 해당 행을 삭제(본인 소유 행만 삭제 가능하도록 RLS로 강제).
- 삭제 성공 시 리스트와 총합이 즉시 갱신된다.

### US-10. 여러 기기에서 동일 기록 확인 (동기화)
**As a** 사용자 **I want to** 다른 기기에서 로그인해도 같은 기록을 보기 **So that** PC와 휴대폰을 오가며 사용할 수 있다

- 기기 A에서 로그인 후 음식을 기록하면, 기기 B에서 같은 계정으로 로그인 시(또는 새로고침 시) 동일한 오늘의 기록이 조회된다.
- 실시간 자동 갱신(기기 A에서 추가하는 순간 기기 B 화면이 자동으로 바뀌는 것)은 MVP 범위가 아니며, **재접속/새로고침 시 최신 데이터를 다시 불러오는 방식**으로 충분하다.

### US-11. 비로그인 접근 제어
**As a** 서비스 운영자 **I want to** 로그인하지 않은 사용자가 트래커 화면에 접근하지 못하게 **So that** 데이터가 계정별로 안전하게 분리된다

- 로그인하지 않은 상태로 접속하면 로그인/회원가입 화면만 노출된다.
- 세션이 없거나 만료된 경우 자동으로 로그인 화면으로 전환된다.

---

## 5. 비기능 요구사항 (Non-Functional Requirements)

| 항목 | 요구사항 |
|---|---|
| 성능 | 검색/계산은 클라이언트에서 처리되어 100ms 이내 반응. 기록 추가/삭제/조회는 Supabase 응답 속도에 의존(일반적으로 수백 ms 이내) |
| 브라우저 호환성 | 최신 Chrome, Safari(모바일 포함) 기준으로 정상 동작 확인 |
| 반응형 디자인 | 모바일(가로 360~400px) ~ 데스크탑 폭에서 레이아웃이 깨지지 않아야 함 |
| 접근성 | 기본적인 키보드 입력(탭 이동, 엔터로 제출)만 지원 |
| 데이터 정확성 고지 | "본 앱의 영양성분 수치는 일반적인 평균값 기준 추정치이며, 실제와 오차가 있을 수 있습니다" 문구 고지 |
| **보안 — 인증** | 비밀번호는 Supabase Auth가 해싱/저장을 전담하며, 우리 앱 코드는 평문 비밀번호를 저장하거나 로깅하지 않는다 |
| **보안 — 데이터 접근** | Supabase Row Level Security(RLS)를 적용하여 각 사용자는 `auth.uid()`가 본인인 행만 조회/추가/삭제할 수 있다 |
| **보안 — 키 관리** | 프론트엔드에는 Supabase anon(public) key만 사용하며, service_role key는 절대 클라이언트 코드에 포함하지 않는다 |
| 개인정보 | 수집되는 개인정보는 이메일 주소(가입용)뿐이며, Supabase 인프라(호스팅 리전 등)에 저장됨을 인지 |
| 가용성 | GitHub Pages(프론트) + Supabase 무료 티어(백엔드) 특성상 별도 SLA 없음. Supabase 무료 프로젝트는 일정 기간 미사용 시 자동 일시정지될 수 있음 (README에 고지) |

---

## 6. 사용자 흐름 (User Flow)

### 6.1 인증 흐름
```
[앱 접속]
   ↓
세션 있음? ──아니오──▶ [로그인/회원가입 화면]
   │                        │
   예                  [회원가입] → (이메일 인증 필요 시 안내) → [로그인]
   ↓                        ↓
[트래커 메인 화면] ◀────────┘
```

### 6.2 기록 흐름
```
[트래커 메인 화면] — 상단 총합, 오늘의 기록(Supabase에서 로드)
   ↓
[음식 이름 검색] → [검색 결과에서 선택]
   ↓
[섭취량 입력 (기본값 자동 채움, 수정 가능)]
   ↓
[계산 미리보기 확인] → [기록에 추가 클릭] → Supabase에 저장
   ↓
[리스트에 반영 + 총합 갱신] → (반복)
```

### 6.3 예외/보조 흐름
```
회원가입 시 이메일 중복 → 에러 안내
로그인 실패(비밀번호 오류) → 에러 안내
검색 결과 없음 → 안내 문구 → 검색어 수정 유도
섭취량 미입력/0 이하 → 추가 버튼 비활성화
기록 저장/삭제 중 네트워크 오류 → 에러 안내 + 재시도 유도
세션 만료 → 자동으로 로그인 화면 전환
자정 경과 후 재접속 → 새 날짜 기준 빈 상태로 시작 (이전 기록은 DB에 보존, 조회 UI는 향후 확장)
```

---

## 7. 화면 설계 (Wireframe 수준)

### 7.1 로그인 / 회원가입 화면
```
┌─────────────────────────────────────┐
│           칼로리 트래커                │
│                                       │
│  이메일:    [_____________________]  │
│  비밀번호:  [_____________________]  │
│                                       │
│           [ 로그인 ]                  │
│      계정이 없으신가요? [회원가입]      │
└─────────────────────────────────────┘
```
- 회원가입 화면은 비밀번호 확인 필드가 추가된 유사한 레이아웃.

### 7.2 트래커 메인 화면
```
┌─────────────────────────────────────┐
│  칼로리 트래커   user@email.com [로그아웃] 2026-09-08 │  ← 헤더
├─────────────────────────────────────┤
│  오늘의 총합                          │
│  ┌───────┬───────┬───────┬───────┐ │
│  │ 칼로리 │ 탄수화물│ 단백질 │  지방  │ │
│  │ 0 kcal │  0 g  │  0 g  │  0 g  │ │
│  └───────┴───────┴───────┴───────┘ │
├─────────────────────────────────────┤
│  음식 검색: [___________________]    │
│  ┌─────────────────────────────┐    │
│  │ 검색 결과 리스트 (최대 8~10개)│    │
│  └─────────────────────────────┘    │
│  선택된 음식: 닭가슴살 (구이)          │
│  섭취량: [ 150 ] g                   │
│  → 예상: 248kcal / 탄0g·단47g·지5g   │
│  [ 기록에 추가 ]                     │
├─────────────────────────────────────┤
│  오늘의 기록                          │
│  ┌─────────────────────────────┐    │
│  │ 흰쌀밥 210g  273kcal  ...  ×│    │
│  │ 닭가슴살 150g 248kcal ...  ×│    │
│  └─────────────────────────────┘    │
├─────────────────────────────────────┤
│  ※ 영양성분은 추정치이며 실제와 다를   │
│    수 있습니다                       │
└─────────────────────────────────────┘
```

### 7.3 모바일 레이아웃 원칙
- 요약 카드 4개는 2x2 그리드로 전환
- 헤더의 이메일/로그아웃은 모바일에서 축약 표시(예: 이메일 대신 아이콘) 검토
- 검색/입력 영역과 기록 리스트는 세로로 풀 너비 스택

---

## 8. 제약사항, 가정, 리스크

### 8.1 제약사항
- 음식 데이터는 자체 조사한 한정된 목록(80~100개)만 지원.
- 비밀번호 재설정, 소셜 로그인은 이번 MVP에 포함되지 않음 — 비밀번호를 잊으면 재가입하거나 Supabase 대시보드에서 수동 처리 필요.
- 실시간 동기화(기기 간 즉시 반영)는 없음 — 재접속/새로고침 필요.

### 8.2 가정
- 사용자는 대부분 모바일 브라우저 또는 데스크탑 Chrome/Safari를 사용한다.
- 하루 기록 건수는 대략 3~10건 내외로, 리스트 성능 문제는 없다고 가정.
- 초기 음식 데이터는 식약처 등 공개된 일반 영양정보를 참고해 수동으로 입력한다.
- Supabase 프로젝트 생성 및 이메일 인증 설정(사용 여부)은 사용자가 Supabase 대시보드에서 직접 진행한다 (계정 생성은 본인이 해야 하는 영역).

### 8.3 리스크 및 대응
| 리스크 | 영향 | 대응 |
|---|---|---|
| 원하는 음식이 목록에 없음 | 사용자가 기록을 포기할 수 있음 | MVP는 자주 먹는 음식 위주로 최대한 커버, 향후 API/커스텀 등록으로 확장 |
| 비밀번호 분실 시 재설정 불가 | 계정 접근 불가 | MVP의 알려진 제약으로 README에 명시, 다음 단계에서 Supabase 비밀번호 재설정 기능 추가 |
| RLS 설정 누락/오류 | 다른 사용자 기록 노출 위험 (심각) | `logs` 테이블에 RLS 필수 적용 및 배포 전 다른 계정으로 교차 확인 테스트 |
| Supabase 무료 티어 비활성화(장기 미사용 시 자동 일시정지) | 서비스 일시 중단 | README에 알려진 제약사항으로 고지 |
| 실수로 삭제 (되돌리기 없음) | 사용자 불편 | MVP는 단순성 우선, 필요 시 향후 "실행취소" 검토 |
| 영양정보 수치 오차 | 신뢰도 저하 | 화면에 "추정치" 고지 문구 명시 |

---

## 9. 향후 확장 로드맵 (이번 구현 범위 아님)
1. 비밀번호 재설정(이메일 발송), 소셜 로그인(Google 등)
2. 공공 영양성분 API(예: 식약처 식품영양성분 DB) 연동으로 음식 종류 대폭 확장
3. 사용자 커스텀 음식 등록/수정 기능
4. 날짜별 히스토리 조회(달력) 및 주간/월간 통계·그래프
5. 목표 칼로리/영양소 설정 및 달성률 표시
6. 기록 실행취소(undo), 즐겨찾기/최근 사용 음식
7. 실시간 동기화 (Supabase Realtime을 이용한 기기 간 즉시 반영)

---

## 10. 다음 단계
1. 이 PRD 리뷰 및 확정
2. [TECH_SPEC.md](TECH_SPEC.md)에 Supabase 스키마/RLS/인증 흐름 반영 (진행 예정)
3. Supabase 프로젝트 생성 (사용자가 직접 — 계정 생성 영역)
4. [rubilisa08/my-product_MVP](https://github.com/rubilisa08/my-product_MVP) 저장소에 문서 커밋 & 푸시
5. [MVP_SCOPE.md](MVP_SCOPE.md) 기준으로 실제 구현 착수
