# PRD (Product Requirements Document) — 칼로리 트래커 MVP

관련 문서: [MVP_SCOPE.md](MVP_SCOPE.md) (기능 체크리스트) · [TECH_SPEC.md](TECH_SPEC.md) (기술 설계)

## 0. 문서 정보
- 작성일: 2026-09-08
- 버전: v4.0 (월간 캘린더 + 목표 칼로리 반영)
- 작성자: Lisa
- 상태: 초안 — 리뷰 후 확정되면 구현 착수
- 저장소: [rubilisa08/my-product_MVP](https://github.com/rubilisa08/my-product_MVP)

### v4.0 변경 요약
- 로드맵으로 미뤄뒀던 "날짜별 히스토리 조회(달력)"와 "목표 칼로리 설정"을 이번 MVP 범위로 앞당김.
- **목표 칼로리**: 하루 총 칼로리 기준 단일 목표치만 설정 (탄/단/지 개별 목표는 이번 범위 아님).
- **달력**: 실제 월력(月曆) 형태 — 요일별 그리드로 표시하고 이전/다음 달로 이동 가능.
- **색상 표시**: 2단계 — 목표 이내(기본색) / 목표 초과(노란색). 기록이 없는 날은 특별히 강조하지 않음.
- 과거 날짜의 기록은 **조회만 가능**하며 수정/삭제는 오늘 날짜 기록에서만 가능 (8.1 제약사항 참고).

---

## 1. 제품 개요

### 1.1 제품명 (가칭)
칼로리 트래커 (Calorie Tracker)

### 1.2 한 줄 정의
로그인 후, 오늘 먹은 음식을 검색해서 섭취량만 입력하면 칼로리와 탄수화물·단백질·지방(탄단지)을 자동 계산해 하루 총량을 보여주고, 달력에서 하루 목표 칼로리 대비 매일의 섭취량을 한눈에 확인할 수 있는 웹 앱.

### 1.3 비전 (장기 방향)
"기록의 번거로움 때문에 식단 관리를 포기하지 않도록" 만드는 가장 가벼운 칼로리 트래커. 계정 기반으로 기록을 안전하게 보관하고, 달력을 통해 꾸준함을 시각적으로 확인하며, 이후 풍부한 음식 DB와 상세 통계로 확장한다.

### 1.4 배경 및 문제 정의
- 식단을 관리하려는 사람은 매 끼니마다 (1) 음식의 100g당 영양성분을 검색하고 (2) 실제 먹은 양에 맞춰 손으로 비율 계산을 하고 (3) 하루치를 다시 합산해야 하는 3단계 수작업을 거친다.
- 하루 단위 기록만으로는 "내가 꾸준히 목표를 지키고 있는지"를 파악하기 어렵다 — 한 달 전체를 놓고 봤을 때 목표를 초과한 날이 며칠인지 한눈에 보이는 시각적 피드백이 필요하다.
- 개인 기록이므로 기기를 바꿔도(휴대폰 ↔ PC) 같은 기록을 볼 수 있어야 하며, 이를 위해서는 계정(로그인) 기반 저장이 필요하다.
- 로그인 기능은 보안이 중요한 영역이므로 직접 구현하기보다 **검증된 인증 서비스(Supabase Auth)** 를 사용한다.

### 1.5 타겟 사용자 / 페르소나

**페르소나 A — "가볍게 확인하고 싶은 나"**
- 특징: 매일 엄격하게 식단을 재는 건 아니지만, 오늘 하루 대략 얼마나 먹었는지 감을 잡고 싶음
- 니즈: 간단한 가입 후, 음식 검색 → 몇 번의 클릭/입력만으로 기록 완료
- 사용 빈도: 하루 2~4회 (끼니/간식마다), PC와 휴대폰을 오가며 사용

**페르소나 B — "운동/다이어트를 막 시작한 사람"**
- 특징: 하루 목표 칼로리를 정해두고 지키려 하지만, 매일 얼마나 지켰는지 되돌아볼 방법이 마땅치 않음
- 니즈: 목표 칼로리를 넘긴 날이 한 달에 며칠인지 달력으로 한눈에 보고 싶음
- 사용 빈도: 다이어트/운동 기간 동안 매일 기록 + 주기적으로 달력을 열어 되돌아봄

이번 MVP는 이 두 페르소나가 공통으로 원하는 "빠른 입력 + 자동 계산 + 하루 합계 확인 + 기기 간 동기화 + 목표 대비 월간 추이 확인"에 집중한다.

---

## 2. 목표 및 성공 지표

### 2.1 제품 목표
1. **빠른 기록**: 음식 1건을 기록하는 데 걸리는 시간을 최소화한다.
2. **정확한 계산**: 입력한 섭취량(g)에 정확히 비례한 칼로리·탄단지를 계산한다.
3. **한눈에 파악**: 하루 총 섭취량(칼로리/탄/단/지)을 별도 조작 없이 즉시 확인할 수 있다.
4. **안전한 개인 계정**: 검증된 인증 서비스로 회원가입/로그인을 제공하고, 본인 기록만 본인이 볼 수 있도록 보장한다.
5. **기기 간 동기화**: 로그인하면 어느 기기에서 접속하든 동일한 기록을 확인할 수 있다.
6. **목표 대비 추이 파악**: 하루 목표 칼로리를 설정하고, 한 달 동안 초과한 날을 달력에서 시각적으로 즉시 파악할 수 있다.

### 2.2 성공 지표 (MVP 기준, 정량)
| 지표 | 목표 |
|---|---|
| 음식 1건 기록 소요 시간 | 10초 이내 (검색 시작 ~ 리스트 반영까지, 로그인 이후 기준) |
| 계산 오차 | 0% — 섭취량 비례 계산은 항상 정확해야 함 (반올림 오차 제외) |
| 초기 음식 데이터 커버리지 | 자주 먹는 한식·배달음식·프랜차이즈 메뉴 기준 100개 이상 등록 |
| 회원가입~로그인 성공 | 이메일/비밀번호로 가입 후 즉시(또는 이메일 인증 후) 로그인되어 트래커 화면 진입 가능 |
| 데이터 격리 | 다른 사용자의 기록이 절대 보이지 않음 (RLS로 검증) |
| 기기 간 동기화 | PC에서 추가한 기록이 재접속 시 모바일에서 동일하게 조회됨 |
| 목표 설정~달력 반영 | 목표 칼로리를 설정하면, 해당 월의 각 날짜 총 칼로리와 비교해 초과 여부가 달력에 정확히 반영됨 |
| 배포 상태 | GitHub Pages 배포 URL로 외부에서 접속 가능 |
| 브라우저 호환 | 최신 Chrome, Safari(모바일 포함)에서 정상 동작 |

### 2.3 이번 MVP가 아닌 것 (Non-goals)
- 의학적으로 정밀한 영양 관리 도구가 되는 것 (참고용 추정치 제공이 목적)
- 다양한 음식/브랜드 제품까지 모두 포괄하는 것 (2단계 이후)
- 비밀번호 재설정, 소셜 로그인(Google 등), 이메일 인증 커스터마이징 등 고급 인증 기능
- 탄수화물/단백질/지방 개별 목표 설정 및 달성률 (칼로리 목표만 지원)
- 주간/월간 통계 그래프, 평균값 분석 (달력의 날짜별 색상 표시로 대체)
- 과거 날짜 기록의 수정/추가 (조회만 가능, 8.1 참고)

---

## 3. 범위 (Scope)

### 3.1 MVP 포함 (In Scope)
- **Supabase Auth 기반 회원가입 / 로그인 / 로그아웃** (이메일 + 비밀번호)
- 로그인하지 않은 사용자는 트래커 화면에 접근할 수 없고, 로그인/회원가입 화면으로 안내됨
- 자체 제작 음식 데이터베이스 (정적 JSON, 100g 기준 정규화된 영양성분 — 한식/배달음식/프랜차이즈 메뉴 포함)
- 음식 검색 및 선택
- 섭취량(g) 입력에 따른 칼로리/탄수화물/단백질/지방 자동 계산
- 오늘 하루 기록을 Supabase DB(logs 테이블)에 사용자 계정 기준으로 저장/조회/삭제
- 하루 총합(칼로리/탄/단/지) 실시간 표시
- 로그인 상태 유지 및 여러 기기에서 동일 계정으로 접속 시 동일 기록 조회
- 날짜가 바뀌면 자동으로 새 하루 기록으로 전환
- **하루 목표 칼로리 설정** (사용자가 숫자 하나로 설정, 언제든 변경 가능)
- **월간 캘린더 화면**: 실제 달력 형태(요일별 그리드)로 이전/다음 달 이동 가능
  - 기록이 있는 날짜에는 그날의 총 칼로리 표시
  - **목표 칼로리를 초과한 날짜는 노란색으로 강조 표시**, 목표 이내인 날짜는 기본색
  - 기록이 없는 날짜는 특별한 강조 없이 표시
  - 날짜를 클릭하면 해당 날짜에 기록한 음식 목록을 조회 가능 (읽기 전용)
- 반응형(모바일 포함) 레이아웃
- GitHub Pages를 통한 정적 웹 배포

### 3.2 MVP 제외 (Out of Scope → 향후 로드맵, 9장 참고)
- 비밀번호 재설정(이메일 발송), 소셜 로그인(Google/Kakao 등)
- 사용자가 새 음식을 직접 등록/편집하는 기능
- 외부 영양성분 공공 API 연동 (음식 종류 확장)
- 탄수화물/단백질/지방 개별 목표 설정 및 달성률 표시
- 주간/월간 통계 그래프 (평균 칼로리 추이 등)
- 과거 날짜 기록의 수정/추가 (조회만 가능)
- 다국어 지원, 다크모드
- 모바일 네이티브 앱화
- 프로필 수정, 회원 탈퇴 UI
- 봇 남용 방지: CAPTCHA(hCaptcha/Turnstile 연동), Rate Limit 세부 조정 — Supabase 대시보드 설정으로 별도 진행 중 (코드 변경 필요 시 추가 반영)

세부 기능 우선순위(Must/Should/Won't)는 [MVP_SCOPE.md](MVP_SCOPE.md)에 체크리스트로 정리되어 있다.

---

## 4. 기능 요구사항 (User Stories & 상세 인수 조건)

### US-1. 회원가입
**As a** 신규 사용자 **I want to** 이메일과 비밀번호로 가입 **So that** 내 기록을 저장할 계정을 만들 수 있다
- 이메일, 비밀번호(및 비밀번호 확인) 입력 필드 제공. 비밀번호는 8자 이상 + 대/소문자·숫자·특수문자 중 3종류 이상 포함해야 함.
- Supabase Auth의 `signUp` 기능을 사용하며, 비밀번호는 우리 앱 코드가 직접 다루거나 저장하지 않는다.
- 가입 성공 시: (Supabase 프로젝트 설정에 따라) 이메일 인증이 필요하면 안내 문구 표시, 인증이 필요 없으면 즉시 로그인 상태로 전환.
- 이미 가입된 이메일로 재가입 시도, 유출된 것으로 알려진 비밀번호(HaveIBeenPwned 연동) 사용 시 등 Supabase가 반환하는 에러를 사용자에게 알기 쉬운 문구로 보여준다.

### US-2. 로그인
**As a** 기존 사용자 **I want to** 이메일과 비밀번호로 로그인 **So that** 내 기록에 다시 접근할 수 있다
- 로그인 성공 시 트래커 메인 화면으로 이동, 실패 시 에러 메시지 표시.
- 로그인 세션은 브라우저에 유지되어 재방문 시 자동 로그인 상태 유지.

### US-3. 로그아웃
**As a** 로그인한 사용자 **I want to** 로그아웃 **So that** 공용 기기에서 내 계정을 보호할 수 있다

### US-4. 음식 검색
**As a** 사용자 **I want to** 음식 이름을 검색 **So that** 매번 영양정보를 직접 찾지 않아도 된다
- 검색창 입력 즉시 부분 문자열 매칭으로 필터링, 최대 8~10개 노출, 결과 없으면 안내 문구 표시.

### US-5. 음식 선택 및 섭취량 입력
**As a** 사용자 **I want to** 음식을 고르고 실제 먹은 양을 입력 **So that** 그 양에 맞는 영양정보를 얻는다
- 선택 시 100g당 기준 영양정보 표시, 섭취량은 `commonServingG` 기본값으로 자동 채워지며 수정 가능.
- 섭취량 0 이하/미입력 시 "추가" 버튼 비활성화.

### US-6. 계산 미리보기 및 기록 추가
**As a** 사용자 **I want to** 추가하기 전에 계산 결과를 먼저 확인 **So that** 잘못된 값으로 기록되는 것을 방지한다
- 실시간 계산 미리보기 표시, "기록에 추가" 클릭 시 Supabase `logs` 테이블에 저장, 성공 시 리스트에 즉시 반영.

### US-7. 오늘의 기록 확인
**As a** 사용자 **I want to** 오늘 기록한 항목을 목록으로 확인 **So that** 무엇을 얼마나 먹었는지 되짚어볼 수 있다
- 오늘 날짜의 본인 기록만 조회, 기록 없으면 빈 상태 문구 표시.

### US-8. 하루 총합 확인
**As a** 사용자 **I want to** 오늘 섭취한 총 칼로리/탄/단/지를 확인 **So that** 하루 전체를 파악한다
- 항목 추가/삭제 시 총합 즉시 재계산.

### US-9. 기록 삭제 (오늘 기록만)
**As a** 사용자 **I want to** 잘못 추가한 오늘 기록을 삭제 **So that** 정확한 하루 기록을 유지한다
- 오늘 날짜 기록에 한해 삭제(×) 버튼 제공, RLS로 본인 소유 행만 삭제 가능.

### US-10. 여러 기기에서 동일 기록 확인 (동기화)
**As a** 사용자 **I want to** 다른 기기에서 로그인해도 같은 기록을 보기 **So that** PC와 휴대폰을 오가며 사용할 수 있다
- 재접속/새로고침 시 최신 데이터를 다시 불러오는 방식 (실시간 자동 갱신은 범위 아님).

### US-11. 비로그인 접근 제어
**As a** 서비스 운영자 **I want to** 로그인하지 않은 사용자가 트래커 화면에 접근하지 못하게 **So that** 데이터가 계정별로 안전하게 분리된다

### US-12. 목표 칼로리 설정
**As a** 사용자 **I want to** 하루 목표 칼로리를 숫자로 설정 **So that** 내가 얼마나 먹어야 하는지 기준을 가질 수 있다
- 인수 조건:
  - 트래커 화면 또는 캘린더 화면에서 목표 칼로리(kcal)를 입력/수정할 수 있다.
  - 처음 로그인한 사용자는 목표가 설정되어 있지 않을 수 있으며, 이 경우 달력의 초과 표시(US-14)는 적용되지 않는다 (목표 미설정 안내 문구 표시).
  - 목표치는 계정에 저장되어 기기와 상관없이 동일하게 적용된다.
  - 목표는 언제든 변경 가능하며, 변경 시점 이후 달력 표시부터 새 목표 기준으로 반영된다 (과거 날짜의 판정 기준을 소급 변경할지는 8.1 참고).

### US-13. 캘린더에서 월별 기록 확인
**As a** 사용자 **I want to** 달력에서 한 달치 기록을 한눈에 확인 **So that** 꾸준히 기록/관리하고 있는지 파악할 수 있다
- 인수 조건:
  - 요일별 그리드(일~토)로 구성된 실제 월력 형태로 표시된다.
  - 상단에 "◀ 이전 달 / 다음 달 ▶" 이동 버튼과 "YYYY년 M월" 표시가 있다.
  - 기록이 있는 날짜 칸에는 해당 날짜의 총 칼로리(kcal)가 작게 표시된다.
  - 기록이 없는 날짜는 칼로리 표시 없이 날짜 숫자만 보인다.
  - 오늘 날짜는 다른 날짜와 구분되는 표시(예: 테두리 강조)를 한다.

### US-14. 목표 초과 날짜 시각적 표시
**As a** 사용자 **I want to** 목표를 초과한 날을 달력에서 색으로 바로 알아보기 **So that** 한 달간의 경향을 빠르게 파악할 수 있다
- 인수 조건:
  - 목표 칼로리가 설정되어 있고, 특정 날짜의 총 칼로리가 목표를 초과하면 해당 날짜 칸이 **노란색**으로 표시된다.
  - 목표 이내인 날짜(기록이 있는 날짜 한정)는 기본 색상으로 표시된다.
  - 기록이 없는 날짜는 초과 여부를 판단할 수 없으므로 색상 강조를 하지 않는다.
  - 목표가 설정되어 있지 않으면 어떤 날짜도 노란색으로 표시되지 않는다.

### US-15. 특정 날짜 상세 기록 조회 (읽기 전용)
**As a** 사용자 **I want to** 달력에서 날짜를 클릭해 그날 무엇을 먹었는지 보기 **So that** 특정 날의 식단을 되돌아볼 수 있다
- 인수 조건:
  - 캘린더에서 날짜를 클릭하면 해당 날짜에 기록된 음식 목록(음식명/섭취량/칼로리/탄단지)과 그날의 총합이 표시된다.
  - 오늘이 아닌 날짜의 기록은 삭제/수정 버튼이 노출되지 않는다 (조회 전용, 3.2 참고).
  - 기록이 없는 날짜를 클릭하면 "이 날은 기록이 없어요" 안내가 표시된다.

---

## 5. 비기능 요구사항 (Non-Functional Requirements)

| 항목 | 요구사항 |
|---|---|
| 성능 | 검색/계산은 클라이언트에서 처리되어 100ms 이내 반응. 캘린더는 월 단위로 로그를 한 번에 조회해 렌더링 (일자별 개별 조회 지양) |
| 브라우저 호환성 | 최신 Chrome, Safari(모바일 포함) 기준으로 정상 동작 확인 |
| 반응형 디자인 | 모바일(가로 360~400px) ~ 데스크탑 폭에서 레이아웃이 깨지지 않아야 함 (달력 포함) |
| 접근성 | 기본적인 키보드 입력(탭 이동, 엔터로 제출)만 지원 |
| 데이터 정확성 고지 | "본 앱의 영양성분 수치는 일반적인 평균값 기준 추정치이며, 실제와 오차가 있을 수 있습니다" 문구 고지 |
| 보안 — 인증 | 비밀번호는 Supabase Auth가 해싱/저장을 전담. 8자 이상 + 복잡도 요구사항 및 유출 비밀번호 차단(HaveIBeenPwned) 적용 |
| 보안 — 봇 방지 | 회원가입/로그인에 CAPTCHA(Cloudflare Turnstile) 적용, Supabase Rate Limit으로 대량 요청 차단 |
| 보안 — 데이터 접근 | Supabase RLS로 각 사용자는 `auth.uid()`가 본인인 행만 조회/추가/삭제 가능. 목표 칼로리 값도 본인만 조회/수정 가능해야 함 |
| 보안 — 키 관리 | 프론트엔드에는 Supabase anon(public) key만 사용, service_role key는 절대 클라이언트 코드에 포함하지 않음 |
| 개인정보 | 수집되는 개인정보는 이메일 주소뿐이며, Supabase 인프라에 저장됨을 인지 |
| 가용성 | GitHub Pages(프론트) + Supabase 무료 티어(백엔드) 특성상 별도 SLA 없음 |

---

## 6. 사용자 흐름 (User Flow)

### 6.1 인증 흐름
```
[앱 접속]
   ↓
세션 있음? ──아니오──▶ [로그인/회원가입 화면] (CAPTCHA 포함)
   │                        │
   예                  [회원가입] → (이메일 인증 필요 시 안내) → [로그인]
   ↓                        ↓
[트래커 메인 화면] ◀────────┘
```

### 6.2 기록 흐름
```
[트래커 메인 화면] — 상단 총합, 오늘의 기록(Supabase에서 로드)
   ↓
[음식 이름 검색] → [검색 결과에서 선택]
   ↓
[섭취량 입력 (기본값 자동 채움, 수정 가능)]
   ↓
[계산 미리보기 확인] → [기록에 추가 클릭] → Supabase에 저장
   ↓
[리스트에 반영 + 총합 갱신] → (반복)
```

### 6.3 캘린더 흐름
```
[트래커 화면] → [캘린더 보기 진입]
   ↓
목표 칼로리 설정됨? ──아니오──▶ [목표 설정 안내/입력]
   │
   예
   ↓
[해당 월 로그를 한 번에 조회] → [날짜별 총 칼로리 계산]
   ↓
[날짜 칸 렌더링: 초과 → 노란색 / 이내 → 기본색 / 기록 없음 → 무강조]
   ↓
[날짜 클릭] → [그날의 음식 목록 상세 조회 (읽기 전용)]
   ↓
["◀ 이전 달" / "다음 달 ▶"] → 다른 월 조회 (반복)
```

### 6.4 예외/보조 흐름
```
회원가입 시 이메일 중복/유출된 비밀번호 사용 → 에러 안내
로그인 실패(비밀번호 오류) → 에러 안내
검색 결과 없음 → 안내 문구 → 검색어 수정 유도
섭취량 미입력/0 이하 → 추가 버튼 비활성화
기록 저장/삭제 중 네트워크 오류 → 에러 안내 + 재시도 유도
세션 만료 → 자동으로 로그인 화면 전환
자정 경과 후 재접속 → 새 날짜 기준 빈 상태로 시작
목표 미설정 상태에서 캘린더 진입 → 노란색 표시 없이 칼로리 숫자만 표시 + 목표 설정 유도 문구
```

---

## 7. 화면 설계 (Wireframe 수준)

### 7.1 로그인 / 회원가입 화면
```
┌─────────────────────────────────────┐
│           칼로리 트래커                │
│                                       │
│  이메일:    [_____________________]  │
│  비밀번호:  [_____________________]  │
│  [ CAPTCHA 위젯 ]                     │
│                                       │
│           [ 로그인 ]                  │
│      계정이 없으신가요? [회원가입]      │
└─────────────────────────────────────┘
```

### 7.2 트래커 메인 화면
```
┌─────────────────────────────────────┐
│  칼로리 트래커  user@email.com [로그아웃] │
│  2026-09-08                [ 📅 캘린더 ] │
├─────────────────────────────────────┤
│  오늘의 총합 (목표: 1800kcal)          │
│  ┌───────┬───────┬───────┬───────┐ │
│  │ 칼로리 │ 탄수화물│ 단백질 │  지방  │ │
│  │ 0 kcal │  0 g  │  0 g  │  0 g  │ │
│  └───────┴───────┴───────┴───────┘ │
├─────────────────────────────────────┤
│  음식 검색 / 기록 추가 (기존과 동일)   │
├─────────────────────────────────────┤
│  오늘의 기록 (기존과 동일, 삭제 가능)  │
└─────────────────────────────────────┘
```

### 7.3 캘린더 화면
```
┌─────────────────────────────────────┐
│  ◀   2026년 9월   ▶     목표: 1800kcal│
├───┬───┬───┬───┬───┬───┬───┤
│ 일 │ 월 │ 화 │ 수 │ 목 │ 금 │ 토 │
├───┼───┼───┼───┼───┼───┼───┤
│    │  1 │  2 │  3 │  4 │  5 │  6 │
│    │1650│1900│    │2100│1700│    │
│    │    │[노랑]│   │[노랑]│    │    │
├───┼───┼───┼───┼───┼───┼───┤
│  7 │  8*│  9 │ 10 │ 11 │ 12 │ 13 │
│1500│1800│    │    │    │    │    │
├───┴───┴───┴───┴───┴───┴───┤
│ (* 오늘 날짜 강조 표시)              │
└─────────────────────────────────────┘
     ↓ 날짜 클릭
┌─────────────────────────────────────┐
│  2026-09-02 (총 1900kcal)            │
│  ┌─────────────────────────────┐    │
│  │ 삼겹살 200g   662kcal ...    │    │
│  │ 흰쌀밥 210g   273kcal ...    │    │
│  └─────────────────────────────┘    │
│  (삭제/수정 버튼 없음 — 조회 전용)    │
└─────────────────────────────────────┘
```

### 7.4 모바일 레이아웃 원칙
- 요약 카드 4개는 2x2 그리드로 전환
- 캘린더는 가로 스크롤 없이 화면 폭에 맞춰 축소 (요일 텍스트 축약: 일/월/화... 유지, 칼로리 숫자 폰트 축소)
- 날짜 클릭 시 상세 기록은 모달 또는 화면 하단 시트로 표시

---

## 8. 제약사항, 가정, 리스크

### 8.1 제약사항
- 음식 데이터는 자체 조사한 한정된 목록(100개 이상)만 지원.
- 비밀번호 재설정, 소셜 로그인은 이번 MVP에 포함되지 않음.
- 실시간 동기화(기기 간 즉시 반영)는 없음 — 재접속/새로고침 필요.
- **과거 날짜 기록은 조회만 가능하고 수정/삭제할 수 없음** — 잘못 기록된 과거 데이터를 고칠 방법이 없다는 한계가 있음 (2단계에서 과거 기록 수정 기능 검토).
- **목표 칼로리는 탄단지를 고려하지 않은 단일 숫자**이므로, 칼로리는 낮아도 탄단지 균형이 나쁜 날을 구분해내지 못함.
- 목표 칼로리를 변경하면 이후 조회 시 **과거 날짜의 초과 여부 판정에도 새 목표가 소급 적용**된다 (특정 시점의 목표를 기록해두지 않음 — 가장 단순한 구현 방식을 우선함).

### 8.2 가정
- 사용자는 대부분 모바일 브라우저 또는 데스크탑 Chrome/Safari를 사용한다.
- 하루 기록 건수는 대략 3~10건 내외로, 리스트/캘린더 성능 문제는 없다고 가정.
- 한 달 최대 31일치 로그를 한 번에 조회해도 성능에 문제가 없다고 가정 (건수가 많지 않으므로).
- Supabase 프로젝트 생성, CAPTCHA 제공자(Cloudflare 등) 가입은 사용자가 직접 진행한다.

### 8.3 리스크 및 대응
| 리스크 | 영향 | 대응 |
|---|---|---|
| 원하는 음식이 목록에 없음 | 사용자가 기록을 포기할 수 있음 | 배달음식/프랜차이즈 포함 100개 이상으로 커버, 향후 API 연동으로 확장 |
| 비밀번호 분실 시 재설정 불가 | 계정 접근 불가 | README에 알려진 제약으로 명시, 다음 단계에서 재설정 기능 추가 |
| RLS 설정 누락/오류 | 다른 사용자 기록·목표치 노출 위험 (심각) | `logs`뿐 아니라 목표 칼로리 저장 위치에도 RLS 필수 적용, 배포 전 교차 계정 확인 |
| 목표 소급 변경으로 인한 혼란 | 과거에는 "목표 이내"였던 날이 목표를 낮춘 뒤 "초과"로 바뀌어 보일 수 있음 | MVP는 단순성 우선, 사용자에게 이 동작을 README/도움말로 안내 |
| 과거 기록 수정 불가에 대한 불만 | 잘못 기록한 과거 데이터를 고칠 수 없음 | 8.1에 알려진 제약으로 명시, 2단계에서 검토 |
| 실수로 삭제 (되돌리기 없음, 오늘 기록 한정) | 사용자 불편 | MVP는 단순성 우선 |
| 영양정보 수치 오차 | 신뢰도 저하 | 화면에 "추정치" 고지 문구 명시 |

---

## 9. 향후 확장 로드맵 (이번 구현 범위 아님)
1. 비밀번호 재설정(이메일 발송), 소셜 로그인(Google 등)
2. 공공 영양성분 API 연동으로 음식 종류 대폭 확장
3. 사용자 커스텀 음식 등록/수정 기능
4. 탄수화물/단백질/지방 개별 목표 설정 및 달성률 표시
5. 과거 날짜 기록의 수정/추가 허용
6. 목표 변경 이력 관리 (특정 시점 기준으로 과거 판정을 고정)
7. 기록 실행취소(undo), 즐겨찾기/최근 사용 음식
8. 실시간 동기화 (Supabase Realtime을 이용한 기기 간 즉시 반영)
9. 사진 촬영/업로드로 음식 자동 인식 및 칼로리·탄단지 추정 — 비전 AI API 연동 및 서버리스 함수 필요, 사진만으로는 섭취량 추정 오차가 클 수 있어 "대략적인 추정 + 사용자 확인/수정" 방식으로 설계 검토 필요

---

## 10. 다음 단계
1. 이 PRD 리뷰 및 확정
2. [TECH_SPEC.md](TECH_SPEC.md)에 목표 칼로리 저장 방식, 캘린더용 월별 조회 쿼리, 색상 로직 반영
3. [MVP_SCOPE.md](MVP_SCOPE.md) 체크리스트에 캘린더/목표 설정 항목 추가
4. 코드 구현: 목표 칼로리 설정 UI, 캘린더 화면, 색상 표시 로직
5. GitHub Pages 재배포 및 확인
